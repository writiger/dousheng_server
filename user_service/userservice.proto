syntax = "proto3";

package user;
option go_package="kitex_gen";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service UserCenter {
  rpc Ping(Request) returns(Response);
}